# Pi Zero Camera System Requirements with Network Share
# Install with: pip3 install -r requirements.txt

# Core dependencies
requests>=2.25.0
Pillow>=8.0.0

# Optional dependencies (usually installed via apt)
# picamera>=1.13
# RPi.GPIO>=0.7.0
# spidev>=3.5